using System.Collections.Generic;

namespace DeveloperConsole
{
    public class #SCRIPTNAME# : Command
    {
        public #SCRIPTNAME#()
        {
            // Fill with all valid invoking command words
            commandWords = new string[]
            {
                
            };

            // Follow the syntax and fill this out
            help = new CommandHelp
            (
                "name",
                "General description.",
                commandWords,
                new List<CommandUsage>
                {
                    // Add command usages here. See ConfigCommand for example
                }
            );
        }

        public override bool Execute(string[] args)
        {
            if (InvalidArgs(args)) return false;

            // Set member var <output> to a message to log, if any
            
            return true;
        }
    }
}